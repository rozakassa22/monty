#include "monty.h"
/**
 * f_nop - does nothing
 * @head: stack head
 * @counter: line_number
 * Return: no return
 */
void f_nop(stack_t **head, unsigned int counter)
{
	stack_t *temp = *head;
	counter += 0;
	temp = temp;
}
